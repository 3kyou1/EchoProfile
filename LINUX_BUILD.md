# EchoProfile Linux Build

This document is being rewritten for EchoProfile.

For now:
- use `pnpm install`
- use `pnpm tauri:build` for standard builds
- expect Linux packaging names to change as the rebrand continues

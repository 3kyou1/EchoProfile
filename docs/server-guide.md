# EchoProfile Server Guide

Server-mode documentation is being rewritten for EchoProfile.

Before publishing this guide, decide:
- whether the server binary will remain `echo-profile --serve`
- what auth env var name to document
- what repository and release URLs to expose
